[package]
name = "ai-router-engine"
version = "0.1.0"
edition = "2021"
description = "Decision router for AI Flow platform: normalize, prompt, LLM, parse, policy"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"
