[package]
name = "ai-search-engine"
version = "0.1.0"
edition = "2021"
description = "Semantic search engine with pluggable providers, embedders, and indexes"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"
