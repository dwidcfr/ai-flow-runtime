[package]
name = "ai-flow-engine"
version = "0.1.0"
edition = "2021"
description = "Flow graph engine: parse, validate, and navigate YAML flows"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
serde_yaml = "0.9"
thiserror = "1"

[dev-dependencies]
tempfile = "3"
