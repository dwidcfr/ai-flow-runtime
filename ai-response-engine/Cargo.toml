[package]
name = "ai-response-engine"
version = "0.1.0"
edition = "2021"
description = "Response generation engine for AI Flow platform"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"
